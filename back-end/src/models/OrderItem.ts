export interface OrderItemRow {
  vendorName: string;   // nomfor
  productCode: number;  // codpro
  productName: string;  // nompro
  unitLiteral: string;  // MASK-LITERAL (unisai + C/ + qtdsai)
  quantity: number;     // quanti
}